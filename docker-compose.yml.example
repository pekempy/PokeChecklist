services:
  pokemon-checklist:
    build: .
    container_name: pokemon-checklist
    ports:
      - "8876:8876"
    environment:
      - PORT=8876
    volumes:
      - ./pokemon_checklist.db:/app/pokemon_checklist.db
    restart: unless-stopped
